stacks:

  us-west-2:

    astuary:
      template: static_site.yml

    bob:
      template: static_site.yml

    reckerops-build:
      template: build.rb

    demo:
      template: static_site.yml

    scrabble:
      template: static_site.yml

    reckerops-vault:
      template: vault.rb

    wedding:
      template: static_site.yml
