stacks:

  us-west-2:

    astuary:
      template: static.yml

    bob:
      template: static.yml

    build:
      template: build.yml

    demo:
      template: static.yml

    scrabble:
      template: static.yml

    tranquility:
      template: tranquility.yml

    vault:
      template: vault.yml

    wedding:
      template: static.yml
