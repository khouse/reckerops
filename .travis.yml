sudo: required

services:
  - docker

before_install: secrets/travis.sh

script:
  - docker-compose build
  - docker-compose up cfn
  - docker-compose up dns

notifications:
  email: false

branches:
  only:
    - master
