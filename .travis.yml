sudo: required

services:
  - docker

before_install: secrets/bootstrap.sh

script:
  - docker-compose build
  - docker-compoes run lambda
  - docker-compose run cfn
  - docker-compose run dns
  - docker-compose run kitchen

notifications:
  email: false

branches:
  only:
    - master
