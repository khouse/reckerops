sudo: required

services:
  - docker

before_install: secrets/travis.sh

script:
  - docker-compose build
  - docker-compose run cfn
  - docker-compose run dns
  - docker-compose run kitchen

notifications:
  email: false

branches:
  only:
    - master
