language: ruby
before_install:
  - echo $RECKEROPS_BUILD_SSH_PRIVATE_KEY > ~/.ssh/reckerops-build.pem
  - gem update --system && gem update bundler
env:
  - COMMAND='rubocop'
  - COMMAND='kitchen test debian'
  - COMMAND='kitchen test ubuntu'
script: bundle exec $COMMAND
notifications:
  email: false
